model:
  name: "UNet"
  in_channels: 4
  num_classes: 4

training:
  epochs: 500
  batch_size: 1
  learning_rate: 0.0001
  optimizer: "Adam"
  loss: "DiceLoss"

paths:
  data_dir: "data/processed/"
  models_dir: "models/UNet/"